# UE Agent Backend v0.1.2

Released: 2026-05-12

## Highlights

- Improved Code Review LLM resilience with a compact natural-language retry when structured JSON synthesis fails.
- Expanded Code Review payload compatibility for selected-file objects and inline source aliases used by editor integrations.
- Refreshed local smoke-test guidance, including PowerShell `Method Not Allowed` troubleshooting.
- Refreshed Code Review benchmark report with 20 offline cases.

## Quality Snapshot

- Ruff: passed.
- Unit + contract tests: passed.
- Integration tests: passed.
- Eval tests: passed by local verification.
- Code Review benchmark: recall 1.0, precision 1.0, false positive rate 0.0, clean-case accuracy 1.0.

## Compatibility

- No UE frontend UI change is required.
- Existing `user_view.blocks`, `data.llm_analysis`, and `data.llm_review` rendering remains compatible.
- New diagnostics are optional: `data.llm_review.fallback_mode = "compact_text_retry"` and `data.llm_review.fallback_result`.
